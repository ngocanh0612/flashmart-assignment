
- **Lý thuyết JOIN:** Phân biệt sự khác biệt giữa INNER JOIN (lấy phần giao nhau) và LEFT JOIN (giữ toàn vẹn bảng bên trái và trả về NULL cho bảng phải nếu không khớp).
- **Hàm COUNT:** Thảo luận về việc dùng `COUNT(order_id)` thay vì `COUNT(*)` để xử lý chính xác giá trị NULL trong tập kết quả LEFT JOIN.
